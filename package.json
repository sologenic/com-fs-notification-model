{
  "name": "com-fs-notification-model",
  "version": "1.2.3",
  "description": "Full stack model for Notification System",
  "main": "build/index.js",
  "scripts": {
    "build": "rm -rf build/ && npx tsc"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.8.12",
    "protoc-gen-ts": "^0.8.5",
    "ts-proto": "^1.141.0"
  }
}
